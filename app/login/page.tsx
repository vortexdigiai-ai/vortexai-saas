'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { supabaseBrowser } from '@/lib/supabase';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [register,setRegister]=useState(false);const [error,setError]=useState('');const router=useRouter();async function submit(){setError('');const s=supabaseBrowser();const r=register?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});if(r.error){setError(r.error.message);return}if(register&&r.data.user){await s.from('tiendas').insert({user_id:r.data.user.id,nombre_tienda:'Mi Tienda'});}router.push('/dashboard')}return <main className="flex min-h-screen items-center justify-center px-6"><div className="glass w-full max-w-md rounded-2xl p-8"><div className="text-2xl font-black">Vortex<span className="text-rose-500">AI</span></div><h1 className="mt-8 text-3xl font-bold">{register?'Crear cuenta':'Bienvenido de nuevo'}</h1><div className="mt-6 space-y-4"><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" placeholder="Contraseña" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="text-sm text-red-400">{error}</p>}<button onClick={submit} className="btn btn-primary w-full">{register?'Crear cuenta':'Entrar'}</button></div><button className="mt-5 w-full text-sm text-slate-400" onClick={()=>setRegister(!register)}>{register?'Ya tengo cuenta':'Crear una cuenta'}</button></div></main>}
